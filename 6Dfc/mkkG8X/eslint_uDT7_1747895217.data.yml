codecov:
  require_ci_to_pass: no
  notify:
    wait_for_ci: no

coverage:
  status:
    patch:
      default:
    project:
      default:
        threshold: 2%

comment:
  layout: diff, flags, files
  require_changes: true